<%= render 'documents/search_documents', document: @document %>
<div class="document-lists">
  <div>登録文書一覧<br>[文書をクリックすると詳細情報が表示されます]</div>
</div>
<% if @documents.length !=0 %>
<div class="documents">
  <% @documents.each do |document| %>
    <div class="document">
      <%= link_to document.title, document_path(document.id), method: :get, class: "document-contents" %>
      <%= link_to document.jurisdiction_department, document_path(document.id), method: :get, class: "document-contents" %>
      <%= link_to document.disposal_date, document_path(document.id), method: :get, class: "document-contents" %> 
    </div>
  <% end %>
<% else %>
<div class="none-document">管理対象の文書を登録してください</div>
<% end %>
</div>